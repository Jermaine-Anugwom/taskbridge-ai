FROM python:3.12-slim
WORKDIR /app
COPY pyproject.toml constraints.txt ./
COPY src ./src
COPY fixtures ./fixtures
RUN python -m pip install --no-cache-dir -c constraints.txt .
EXPOSE 8000
CMD ["uvicorn", "taskbridge.api:app", "--host", "0.0.0.0", "--port", "8000"]
